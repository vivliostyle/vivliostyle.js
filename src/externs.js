/**
 * Copyright 2015 Vivliostyle Inc.
 */

/**
 * An enclosing object, to which `vivliostyle` object (namespace) will be attached.
 * The enclosing object is passed from a wrapper function defined in `wrapper.js`.
 */
var enclosingObject;
